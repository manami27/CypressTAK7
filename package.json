{
  "name": "tak_cypress",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "cart": "npx cypress run --spec cypress/e2e/4-taklast/cart.cy.js --browser chrome",
    "takdemo": "npx cypress run --spec cypress/e2e/4-taklast/**.cy.js --browser chrome",
    "takfolder": "npx cypress run --spec cypress/e2e/**tak**/**.cy.js --browser chrome"
  },
  "author": "",
  "license": "ISC",
  "description": "",
  "devDependencies": {
    "cypress": "^13.14.2"
  },
  "dependencies": {
    "tak_cypress": "file:"
  }
}
